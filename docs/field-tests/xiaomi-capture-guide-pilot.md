# Xiaomi capture guide — field evidence record

Status: **NOT RUN / NOT FIELD-RELEASED**. The current source route is additionally
blocked by deterministic geometry preflight. The browser simulation is for
interaction review only. Do not use it as approved photography instructions.

The desktop matrix cannot establish Android tab reclamation, camera round-trip,
LAN access or vendor-browser behavior. Run this matrix after correcting and
reviewing the route. Any failed supported-path row blocks field release.

Record for every run: date/time, phone/Android version, Chrome version, exact
origin URL, capture ID, mode hash, operator and artifact commit. Keep screenshot
or log paths with each observation. No rows below have been executed.

| Origin/path | Action | Expected result | Observed / pass-fail | Evidence / notes |
| --- | --- | --- | --- | --- |
| Stable HTTPS / Chrome | First open, select each mode | Explicit selection and camera checklist; correct counts | Not run | |
| Stable HTTPS / Chrome | Refresh mid-group | Correct progress/cursor; station unconfirmed | Not run | |
| Stable HTTPS / Chrome | Native camera, take JPG, return | Guide remains usable; only manual marks change | Not run | |
| Stable HTTPS / Chrome | Kill/reclaim tab, reopen same URL | Reload restores saved mode/cursor; no station claim | Not run | Record exact Android/ADB procedure and server request log |
| Stable HTTPS / Chrome | Switch modes, refresh, reset one | Independent logs; other mode preserved | Not run | |
| Stable HTTPS / Chrome | Download progress | JSON opens and retains all events; no photos | Not run | |
| Stable HTTPS / Chrome | Force download failure, Clipboard copy | Exact JSON copied; state intact | Not run | Record test method |
| Mac-LAN HTTP / Chrome | First open and reload exact URL | Phone reaches same live origin | Not run | Check Wi-Fi client isolation |
| Mac-LAN HTTP / Chrome | Native camera and return | No false completion or lost saved progress | Not run | |
| Mac-LAN HTTP / Chrome | Kill/reclaim tab and reload | Same route restored; confirmation cleared | Not run | Exact ADB method + server GET evidence required |
| Mac-LAN HTTP / Chrome | Download and legacy copy | Download works, or exact selected copy fallback | Not run | Record permission/browser behavior |
| Both supported origins | Block Clipboard and legacy copy | Readonly JSON remains fully selected for manual copy | Not run | |
| Both supported origins | Make server unreachable then reload | Honest browser load failure; no offline/PWA claim | Not run | |
| Both supported origins | Restore exact same origin | Saved mode and events recover; station unconfirmed | Not run | |
| Both supported origins | Storage denied/full | Persistent warning; capture continues in memory; export works | Not run | Record injection method |
| Direct file/content URL | Open preview | Unverified persistence/download warning; usable fallback | Not run | Unsupported field path |
| Xiaomi Browser | Open preview | Unverified-browser warning; no persistence guarantee | Not run | Unsupported field path |
| WeChat/in-app WebView | Open preview | Warning or readable fallback; no guarantee | Not run | Unsupported field path |

For tab-reclamation evidence, document the actual package name and exact
developer-tool/ADB commands used on this phone. Do not substitute an ordinary
refresh for a killed-tab result. Note whether the server saw a new document
request. Never claim a tab-kill row passed without that evidence.

Record effective image dimensions, focal length/intrinsics and observed framing
during the first real batch. The current 70° horizontal / 55° vertical FOV and
5° side margins are provisional. Calibrate a single camera model before
promoting the route beyond a pilot. Keep both doors, lights and curtains fixed;
changing door state requires a new capture ID.
